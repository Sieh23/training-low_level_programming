#include "main.h"
#include <unistd.h>

/**
 *  mul- checks the code
 *  @a: holds an integer value.
 *  @b: holds an integer value.
 * Return:  is always 0.
 */

int mul(int a, int b)
{
	return (a * b);

	return (0);
}
