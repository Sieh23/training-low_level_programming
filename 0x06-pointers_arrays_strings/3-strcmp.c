#include "main.h"
#include <unistd.h>

int _strcmp(char *s1, char *s2)
{
	int len;

	for(len = 0; len[s1] != '\0' && len[s2] != '\0'; len++)
	{

	}

		  
		if(len[s1] < len[s2])
			return (-15);

		else if (len[s1] > len[s2])
			return (15);

		else 
			return (0);
	

}
